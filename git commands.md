git init
git add <file_name> 
<!-- if want to add the whole folder then -->
git add .

<!-- to checkthe files  -->
git status

git commit -m "your message"

git log
git log --oneline

git diff <filename>

git checkout

